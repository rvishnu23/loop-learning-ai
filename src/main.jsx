import React from "react";
import { createRoot } from "react-dom/client";
import App from "../loop-learning-ai.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);